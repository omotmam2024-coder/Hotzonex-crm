# Supabase project connection (frontend-safe, anon-key only — never the service_role key)
VITE_SUPABASE_URL=https://glhfkbhmkeunvvwuaovx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImdsaGZrYmhta2V1bnZ2d3Vhb3Z4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODk0ODY5MzksImV4cCI6MjEwNTA2MjkzOX0.djFFVALjoK6F3JrAYC1fOlGmK-LmoWyAQirapLo56qg

# Set to "true" to switch campaign dispatch from manual WhatsApp-link mode to
# the provider adapter stub (Twilio / Africa's Talking shaped). Leave "false"
# until a real provider is wired up.
VITE_MESSAGING_PROVIDER_ENABLED=false

# Product name shown in the PWA manifest, browser tab, and first-run setup
# screen — set this per deployment to white-label the app. The company name
# shown everywhere else (sidebar, receipts, invoices) is configured after
# login, in Admin > Settings.
VITE_APP_NAME=CRM
